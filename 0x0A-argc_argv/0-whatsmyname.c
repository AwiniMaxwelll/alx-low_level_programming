#include <stdio.h>
/**
 * main - main entry point
 * @argc: length of argument provided to the function
 * @argv: the array of stings provided to the the function
 * Return: alway 0 (success)
 */
int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", argv[0]);
return (0);
}
