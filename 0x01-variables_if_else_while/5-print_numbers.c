#include <stdio.h>
/**
 * main - prints integers from 1 to 10 inclusive
 * Return: (0) (success)
 */
int main(void)
{
	char num;

	for (num = 0; num <= 9; num++)
	printf("%d", num);
	printf("\n");
	return (0);
}
